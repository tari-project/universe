import styled from 'styled-components';

export const WalletTabWrapper = styled.div`
    overflow: hidden;
    height: 100%;
`;

export const HistoryWrapper = styled.div`
    overflow: hidden;
    border-radius: 10px;
    max-height: min(30vh, 300px);
`;
