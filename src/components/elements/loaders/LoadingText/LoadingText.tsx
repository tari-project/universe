import { AnimatedGradientText } from './styles';

const LoadingText = ({ text }: { text: string }) => {
    return <AnimatedGradientText>{text}</AnimatedGradientText>;
};

export default LoadingText;
