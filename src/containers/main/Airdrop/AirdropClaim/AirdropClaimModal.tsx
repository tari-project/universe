import GreenModal from '@app/components/GreenModal/GreenModal.tsx';

export default function AirdropClaimModal() {
    // const { t } = useTranslation();

    return (
        <GreenModal showModal onClose={() => null}>
            <h1> {'hi'} </h1>
        </GreenModal>
    );
}
