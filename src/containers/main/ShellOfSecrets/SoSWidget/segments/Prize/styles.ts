import styled from 'styled-components';

export const Wrapper = styled('div')`
    height: 32px;
    width: 100%;

    display: flex;
    align-items: center;
    justify-content: center;
    gap: 10px;

    color: #e6ff47;
    font-family: 'IBM Plex Mono', sans-serif;
    font-size: 8.519px;
    font-weight: 700;
    line-height: 129.623%;
    text-transform: uppercase;

    background-color: rgba(230, 255, 71, 0.15);
    border-radius: 7px;
    backdrop-filter: blur(1px);

    flex-shrink: 0;
`;
