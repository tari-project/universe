import styled from 'styled-components';

export const SecondaryAddressesWrapper = styled.div`
    gap: 0;
    display: flex;
    flex-direction: column;
`;

export const ETHAddressWrapper = styled.div`
    gap: 6px;
    display: flex;
    flex-direction: column;
`;
