import styled from 'styled-components';

export const ButtonsWrapper = styled.div(() => ({
    display: 'flex',
    justifyContent: 'right',
    marginTop: '16px',
    alignItems: 'center',
    gap: '16px',
}));
