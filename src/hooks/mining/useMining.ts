import { useBlockInfo } from './useBlockInfo.ts';
// import { useBalanceChanges } from '@app/hooks/mining/useBalanceChanges.ts';

export default function useMining() {
    // temp names, will change later
    useBlockInfo();
    // useBalanceChanges();
}
